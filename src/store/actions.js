import * as actionsTypes from './actionsTypes';

export const setLogin = data => ({ type: actionsTypes.LOGIN, data });

export const setUserInfo = data => ({ type: actionsTypes.USER_INFO, data });

export const setTab = data => ({ type: actionsTypes.TAB, data });

